import {
  mount
} from '@vue/test-utils';
import Sidebar from '@/components/Sidebar.vue';

describe('Sidebar', () => {

  const wrapper = mount(Counter);

  const vm = wrapper.vm;

});



console.log(wrapper);
console.log(Sidebar);